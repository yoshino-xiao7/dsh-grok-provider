## 中文

`1.0.0` 是尚未发布的源码候选，范围是 Search 响应协议修复。它修复 `grok-4.6` 在真实 Web/X Search 流中仍可能触发的 `The Grok provider rejected an invalid or unsupported response`：上游可能在一次 Search 完成后，让同一 reasoning ID 继续出现为多个空占位生命周期；完成态 Web Search 也可能返回 `open_page` action。

> 当前 npm 稳定版和 `latest` 仍是 `0.1.11`。`1.0.0` 已完成版本更新、本地自动化和脱敏真实协议验证；双平台 CI、最终提交、唯一制品冻结、精确制品授权及发布回读仍未完成。本页不得作为已发布证明。

### 安装

当前稳定版：

```sh
dsh plugin --profile web add dsh-grok-provider@0.1.11
dsh web
```

仅在 `1.0.0` 正式发布并完成 Registry 回读后使用：

```sh
dsh plugin --profile web add dsh-grok-provider@1.0.0
dsh web
```

### 修复内容

- 首次复用 reasoning ID 前，原 lifecycle 必须闭合，并且两者之间必须有一个完成的 Web/X server Search。
- ID 一旦得到 Search-backed 证明，后续可以继续出现为多个严格空占位；每次出现都必须有独立的 `response.output_item.done`。
- “严格空”要求 visible summary/content 为空，且不存在 summary/raw lifecycle；允许有界 opaque `encrypted_content`，但它不成为可见 reasoning，也不会导致保存 raw 明文。
- `response.incomplete` 不能吞掉尚未 `output_item.done` 的复用段；所有复用段闭合后，后续 max-token 终态仍按既有契约完成。原项未闭合、首次复用前无完成 Search、跨类型、非空 summary/raw、未知 terminal 字段或 accessor-backed Search item/response container 字段继续失败关闭。
- 完成态 Web Search 只接受精确 `open_page` action 和有界的 `{ type: "open_page", url }`；streamed 与 final action 的 type/URL 必须一致。
- Provider 校验后立即丢弃 `open_page` URL，不打开、不预览、不下载、不 replay，也不把远端 Search 伪装成 Harness 本地工具调用。
- Search terminal item 与外层 response 字段先捕获 own-data 快照再分派、对账；自修改 accessor 不会被调用。ID 已 Search-backed 后的后续复用不再反复扫描先前 Search。

### 真实协议验证

在仓库所有者明确授权测试的已登录账号环境中，候选完成了两层脱敏复验；该测试授权不是精确制品发布授权：

- 原始协议：Web/X 各 1 次请求、各 64 个事件，分别观察到对应 Search，终态均为 `completed`；
- 生产 adapter：direct Web/X 均以 `stop` 完成；
- Harness 形状：同名 `x_search` 三轮依次为 `tool-calls`、`tool-calls`、`stop`，前两轮各 1 次本地调用。连同 direct Web/X 共 5 次 Responses。

探针没有保存或输出搜索结果、URL、prompt、账号身份或凭据。它们证明候选协议方向能完成观察到的真实流程，但不替代自动化、双平台 CI、OAuth、完整桌面会话或 Windows 真机验收。

### 发布前仍需完成

- macOS 14 / Windows 2022 CI 均通过，并把最终 release commit 固定到发布证据；
- 从该 commit 只冻结一个 `dsh-grok-provider-1.0.0.tgz`，记录文件数、大小、SHA-256、npm SRI、隔离安装与 exports smoke；
- 在完整提交和唯一制品证据形成后，取得仓库所有者对该精确制品的明确授权；
- 创建不可变 tag 与唯一 GitHub Release asset，通过 Trusted Publisher 发布同一 tarball；
- 回读 npm `latest`、Registry 字节、signature、attestations 与 SLSA provenance 后，再把本页改为正式发布事实。

### 兼容性与不变边界

- DeepSeek Harness：精确 `0.1.1-rc.2`
- Node.js：`>=24.19.0`
- 平台：macOS arm64、Windows x64；网络可达 Windows 真机外部浏览器弹出仍未验收
- Search 仍默认关闭且仅对精确 `grok-4.6` 开放；结果与 citation 仍是不可信远端内容
- 登录、凭据、模型目录、图片输入、固定 endpoint 与权限边界不变
- 本版不新增图片生成、任意 URL 下载、API Key、多账号、企业 OIDC、ACP 或 Headless wrapper

<details>
<summary>English release notes</summary>

`1.0.0` is an unpublished Search-response protocol repair candidate. It addresses remaining `The Grok provider rejected an invalid or unsupported response` failures in real `grok-4.6` Web/X Search streams: after one completed Search, the upstream may emit multiple empty placeholder lifecycles for the same reasoning ID, and a completed Web Search may return an `open_page` action.

> The current stable release and npm `latest` remain `0.1.11`. Versioning, local automation, and redacted real-protocol verification are complete for `1.0.0`; dual-platform CI, the final commit, unique-artifact freezing, exact-artifact authorization, and release readback are not. This page is not publication evidence.

### Install

Current stable release:

```sh
dsh plugin --profile web add dsh-grok-provider@0.1.11
dsh web
```

Use this only after `1.0.0` is published and read back from the Registry:

```sh
dsh plugin --profile web add dsh-grok-provider@1.0.0
dsh web
```

### Fixes

- Before the first reasoning-ID reuse, the original lifecycle must close and one completed Web/X server Search must lie between the two items.
- Once Search-backed, that ID may continue through multiple strictly empty placeholders, each with its own `response.output_item.done`.
- "Strictly empty" means empty visible summary/content and no summary/raw lifecycle. A bounded opaque `encrypted_content` value is allowed, but is not visible reasoning and does not retain raw plaintext.
- `response.incomplete` cannot swallow a reused lifecycle that has not reached `output_item.done`; after every reused lifecycle closes, a later max-token terminal remains valid. Still-open, pre-Search, cross-type, non-empty summary/raw, unknown-terminal, and accessor-backed Search-item/response-container shapes continue to fail closed.
- A completed Web Search `open_page` action accepts only exact bounded `{ type: "open_page", url }`; streamed and final action type/URL must match.
- The Provider discards the validated URL and never visits, previews, downloads, or replays it, nor projects remote Search as a local Harness tool call.
- Search terminal items and outer response fields are captured as own-data snapshots before dispatch and reconciliation, so self-replacing accessors are never invoked. Later reuses of an already Search-backed ID do not rescan prior Search calls.

### Real protocol verification

With the repository owner's explicit permission to test in the already authenticated account environment, the candidate completed two-layer redacted verification. That test permission is not exact-artifact release authorization:

- Raw protocol: one 64-event response each for Web and X, with the requested Search kind observed and terminal `completed`;
- production adapter: direct Web and direct X both completed with `stop`;
- Harness-shaped flow: the same-name `x_search` sequence ended `tool-calls`, `tool-calls`, then `stop`, with one local call in each of the first two turns. Together with direct Web/X, this made 5 Responses calls.

The probes did not retain or expose results, URLs, prompts, account identity, or credentials. They verify the observed protocol direction, not automation, dual-platform CI, OAuth, a complete desktop session, or real-device Windows acceptance.

### Remaining release gates

- Pass macOS 14 / Windows 2022 CI and bind the final release commit in the evidence.
- Freeze exactly one `dsh-grok-provider-1.0.0.tgz` from that commit, recording file count, sizes, SHA-256, npm SRI, isolated install, and exports smoke.
- Obtain explicit repository-owner authorization for that exact artifact only after the complete commit and artifact evidence exist.
- Create the immutable tag and unique GitHub Release asset, then publish that same tarball through Trusted Publisher.
- Read back npm `latest`, Registry bytes, signatures, attestations, and SLSA provenance before changing this page to published facts.

### Compatibility and unchanged boundaries

- DeepSeek Harness: exact `0.1.1-rc.2`
- Node.js: `>=24.19.0`
- Platforms: macOS arm64 and Windows x64; network-reachable external-browser launch remains unverified on a real Windows device
- Search remains default-off and exact-`grok-4.6` only; results and citations remain untrusted remote content
- Authentication, credentials, model catalog, image input, fixed endpoints, and permission boundaries are unchanged
- Image generation, arbitrary URL downloads, API-key mode, multiple accounts, enterprise OIDC, ACP, and Headless wrapping remain out of scope

</details>
