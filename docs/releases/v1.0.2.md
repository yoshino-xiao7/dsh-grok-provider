## 中文

`1.0.2` 修复一个可见性问题：Web/X Search 响应可能包含多个完整闭合、但没有任何可见内容的 reasoning lifecycle。此前每个空 lifecycle 都会被投影成一行没有内容的 `Think`；本版仍严格校验这些 item，但不再创建空的 Harness block。

精确 `dsh-grok-provider@1.0.2` 已正式发布；npm Registry 回读确认该版本可安装且 `latest=1.0.2`，GitHub Release、Registry 字节、签名、attestations 与 provenance 均已核验。

### 安装或升级

npm Registry 已提供该精确版本，执行：

```bash
dsh plugin --profile web add dsh-grok-provider@1.0.2
```

随后重启 DeepSeek Harness / DeepSeek YukiRyou，并在新回复中验证。已经持久化在旧会话中的空 `Think` 不会被回写删除。

### 修复内容

- reasoning item added 时只建立内部 FSM，不立即发送可见 `block-start`。
- 收到首个非空 summary 或 raw reasoning delta 时才创建 block，之后继续沿用原有 delta 与 block-end 顺序。
- item 完整闭合时仍严格为空，则只完成协议校验并产生零个可见 reasoning chunk。
- 多个真正有内容的 reasoning item 仍保持独立块，不合并、不重排。
- 为等待更早 reasoning 决议而缓存的后续 text/reasoning/tool-call chunk 共享 65,536 条、32 MiB 动态 UTF-8 载荷上限，超限立即失败关闭，释放屏障后归还预算。
- 聚焦回归复现 10 个空 reasoning 与 1 个非空 reasoning 交错，只允许后者生成一个完整 `Think`。

### 严格边界

- 完整闭合的普通非复用空 reasoning 仍执行既有 ID/type own-data、sequence/output index、status、summary/content 空值、encrypted-content 上限与 lifecycle 一致性校验；本版没有为其他字段新增 exact-key/accessor 规则。Search-backed 复用项继续采用更严格的精确 own-data 形状并拒绝未知键/accessor。
- 非空复用、乱序、截断，以及 `response.incomplete` 到达时仍未闭合的 Search-backed 复用段继续失败关闭；普通非复用 open item 保持既有 max-token 行为：没有可见 delta 的空项被隐藏，已有 delta 的 partial 项保留并闭合。后续可见块在更早 reasoning 决议前缓存，并按 output index 发射。
- 正文、工具调用、usage、finish reason、可见非空 reasoning replay、Search 后整响应 replay 抑制、请求编译、认证、模型、图片、固定 endpoint、citation/URL 处理与 Harness 权限层不变。隐藏普通空项不占 replay 对齐槽，其 encrypted content 校验后不持久化。
- 兼容性边界仍为 DeepSeek Harness `0.1.1-rc.2`、Node.js `>=24.19.0`、macOS arm64 与 Windows x64；macOS x64 与 Linux 不支持。
- 本版不保证 X 本体始终可达，也不构成网络可达 Windows 真机外部浏览器弹出验收。

### 真实账号验收

2026-08-30 对候选源码（其生产代码与最终 release commit 逐字节一致）的生产 Adapter 路径各发送一次 `grok-4.6` High Effort 请求。Web Search 实际完成 5 个 Search lifecycle，X Search 实际完成 3 个 custom-tool Search lifecycle；两次均只产生 1 个非空 reasoning block、0 个空 reasoning block、1 个非空 text block 和 1 个 finish。验收只输出计数，没有保存或输出搜索/回复正文、URL、身份、凭据或原始响应。

### 发布证据

- 最终 release commit 为 `be200f9352afe93b27dd2856d89c01674f0cd637`；annotated tag object `b7efd3aabb99c73e1747d2d87890cdf9b284c438` peel 至该提交；final CI run [`33318426571`](https://github.com/yoshino-xiao7/dsh-grok-provider/actions/runs/33318426571) 在 macOS 14 / Windows 2022 全绿。
- Trusted Publisher run [`33319150964` attempt 1](https://github.com/yoshino-xiao7/dsh-grok-provider/actions/runs/33319150964/attempts/1) 发布仓库所有者精确授权的唯一 74 文件 tarball：packed size 255,282 bytes，unpacked size 789,962 bytes；SHA-1 为 `3feddb7048fe4c796037804518999b12ae491802`，SHA-256 为 `010a21770cb3e4e42b7195984df1f5bf8dc5027066198cf99b7d713ac045f605`，SRI 为 `sha512-TcvvPUXBJZEA728pVnUrXSZebGfIoB5ATG5041wA1OFzOE+hFTO98C5Fxl99WuFW2y7V89gkusYIKCpGlLNQIg==`。冻结候选、GitHub Release asset 与 npm Registry tarball 逐字节一致，npm 确认 `dsh-grok-provider@1.0.2` 可安装且 `latest=1.0.2`。
- Node `24.19.0` / npm `11.5.1` 的 Registry 锁定隔离安装通过 Host 注册/`apply`/注入与 client 注册/factory/`apply`/注入 smoke；生产依赖审计为 0 漏洞。`npm audit signatures` 验证安装图中 11 个 Registry 签名包与 2 个 attested 包；本包公开 metadata 含 1 个 Registry signature 与 2 个 attestations。SLSA provenance 精确绑定 `release.yml`、`refs/tags/v1.0.2`、release commit 与 publish run。
- 精确 Node `24.19.0` 本地全量门禁为 265 tests、263 pass、0 fail、2 platform skips；隔离 cache 的 dry-run pack 为 74 文件。

### npm README 流程修正

`1.0.1` 的已发布 tarball 内保留了旧版安装引导，且已发布字节不可覆盖。已发布的 `1.0.2` tarball 将双语 README 与精确 `@1.0.2` 命令一起打包；发布 workflow 直接读取唯一 tarball 内的 README 前言与 Quick Start，避免只修正发布后的主分支。

<details>
<summary>English release notes</summary>

`1.0.2` fixes a presentation defect: Web/X Search responses may contain multiple fully closed reasoning lifecycles with no visible content. Each empty lifecycle previously became a contentless `Think` row. This version continues to validate every item strictly but no longer creates an empty Harness block.

Exact `dsh-grok-provider@1.0.2` is published. npm Registry readback confirms that the version is installable and `latest=1.0.2`, and the GitHub Release, Registry bytes, signatures, attestations, and provenance are verified.

### Install or upgrade

The exact version is available from the npm Registry. Run:

```bash
dsh plugin --profile web add dsh-grok-provider@1.0.2
```

Restart DeepSeek Harness / DeepSeek YukiRyou and verify with a new response. Empty `Think` rows already persisted in older conversations are not rewritten.

### Fix

- `reasoning` item creation establishes only the internal FSM and does not immediately emit a visible `block-start`.
- The first non-empty summary or raw-reasoning delta starts the block; the existing delta and block-end order then remains unchanged.
- An item that closes while remaining strictly empty completes protocol validation and emits no visible reasoning chunk.
- Separate non-empty reasoning items remain separate and retain their order.
- Later text, reasoning, and tool-call chunks waiting on an earlier reasoning decision share aggregate limits of 65,536 chunks and 32 MiB of dynamic UTF-8 payload. Overflow fails closed and releasing the barrier returns the budget.
- A focused regression interleaves 10 empty reasoning lifecycles with 1 non-empty lifecycle and permits only the latter to create one complete `Think` block.

### Strict boundaries

- Completed ordinary non-reused empty reasoning retains the existing ID/type own-data, sequence/output-index, status, empty summary/content, encrypted-content bound, and lifecycle-consistency checks; this version adds no exact-key/accessor rule for its other fields. Search-backed reused items retain their stricter exact own-data shape and unknown/accessor rejection.
- Non-empty reuse, ordering violations, truncation, and an open Search-backed reused lifecycle at `response.incomplete` continue to fail closed. Ordinary non-reused open items retain the existing max-token behavior: an item with no visible delta is hidden, while a partial item with a delta is preserved and closed. Later visible blocks wait for earlier reasoning decisions and emit in output-index order.
- Text, tool calls, usage, finish reasons, visible non-empty reasoning replay, whole-response replay suppression after observed Search, request compilation, authentication, models, images, fixed endpoints, citation/URL handling, and the Harness permission layer are unchanged. A hidden ordinary empty item occupies no replay-alignment slot, so its validated encrypted content is not persisted.
- Compatibility remains DeepSeek Harness `0.1.1-rc.2`, Node.js `>=24.19.0`, macOS arm64, and Windows x64; macOS x64 and Linux are unsupported.
- This version does not guarantee X reachability and is not real-device Windows external-browser acceptance.

### Live-account acceptance

On 2026-08-30, the production Adapter path from the candidate source (whose production code is byte-identical to the final release commit) sent one `grok-4.6` High Effort request for each Search mode. Web Search completed 5 Search lifecycles and X Search completed 3 custom-tool Search lifecycles. Each request produced exactly 1 non-empty reasoning block, 0 empty reasoning block, 1 non-empty text block, and 1 finish. The acceptance emitted only these counts and did not retain or output Search/response text, URLs, identity, credentials, or raw responses.

### Release evidence

- Final release commit `be200f9352afe93b27dd2856d89c01674f0cd637` is the target of annotated tag object `b7efd3aabb99c73e1747d2d87890cdf9b284c438`; final CI run [`33318426571`](https://github.com/yoshino-xiao7/dsh-grok-provider/actions/runs/33318426571) passed on macOS 14 and Windows 2022.
- Trusted Publisher run [`33319150964` attempt 1](https://github.com/yoshino-xiao7/dsh-grok-provider/actions/runs/33319150964/attempts/1) published the repository owner's exactly authorized unique 74-file tarball: 255,282 bytes packed and 789,962 bytes unpacked; SHA-1 is `3feddb7048fe4c796037804518999b12ae491802`, SHA-256 is `010a21770cb3e4e42b7195984df1f5bf8dc5027066198cf99b7d713ac045f605`, and SRI is `sha512-TcvvPUXBJZEA728pVnUrXSZebGfIoB5ATG5041wA1OFzOE+hFTO98C5Fxl99WuFW2y7V89gkusYIKCpGlLNQIg==`. The frozen candidate, GitHub Release asset, and npm Registry tarball are byte-identical, and npm confirms that `dsh-grok-provider@1.0.2` is installable and `latest=1.0.2`.
- A locked Registry install under Node `24.19.0` / npm `11.5.1` passed Host registration/`apply`/injection and client registration/factory/`apply`/injection smoke checks with zero production vulnerabilities. `npm audit signatures` verified 11 Registry-signed packages and 2 attested packages in the installed graph; this package's public metadata contains 1 Registry signature and 2 attestations. SLSA provenance exactly binds `release.yml`, `refs/tags/v1.0.2`, the release commit, and the publish run.
- The exact Node `24.19.0` local suite reports 265 tests, 263 pass, 0 fail, and 2 platform skips; the isolated-cache dry-run pack lists 74 files.

### npm README process correction

The published `1.0.1` tarball retained an older installation instruction and published bytes cannot be replaced. The published `1.0.2` tarball packages the bilingual README with the exact `@1.0.2` command, while the release workflow reads the preamble and Quick Start directly from the unique tarball rather than relying on a post-release branch update.

</details>
